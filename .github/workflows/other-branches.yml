name: Feature and fixes branches

on:
  push:
    branches:
    - feature/**

jobs:
  # check-and-test:
  #   name: Test changes
  #   runs-on: ubuntu-latest

  #   steps:
  #   - name: Checkout repo
  #     uses: actions/checkout@v1

  #   - name: Set up NodeJS
  #     uses: actions/setup-node@v1
  #     with:
  #       node-version: "14.x"

  #   - name: Link cache action
  #     uses: actions/cache@v1
  #     with:
  #       path: ~/.npm
  #       key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
  #       restore-keys: |
  #         ${{ runner.os }}-node-

  #   - name: Install dependencies and build
  #     run: |
  #       cd science-archive-web &&
  #       npm cache clean --force &&
  #       npm install &&
  #       npm install -g @angular/cli &&
  #       ng build

  build-and-deploy:
    name: Build and delpoy service
    runs-on: self-hosted

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up .NET SDK
      uses: actions/setup-dotnet@v1.7.2

    - name: Install backend dependencies
      working-directory: ./ScienceArchive.Server
      run: dotnet restore

    - name: Build solution
      working-directory: ./ScienceArchive.Server
      run: dotnet build --configuration Release --no-restore

    - name: Publish backend
      working-directory: ./ScienceArchive.Server
      run: dotnet publish -c Release

    - name: Clean previous build
      working-directory: /var/www/
      run: sudo rm -rf ./science-archive-api/

    - name: Copy build files
      working-directory: ./ScienceArchive.Server/ScienceArchive.Api/bin/Release/net7.0/
      run: sudo cp -r ./publish /var/www/science-archive-api

    - name: Reload API service
      run: systemctl --user restart science-archive-api
